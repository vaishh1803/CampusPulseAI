# Check installed tags with `ollama list` and set them here
OLLAMA_URL=http://localhost:11434
GRANITE_MODEL=granite3.3:8b
EMBED_MODEL=granite-embedding:278m
# Set to 1 to run without Ollama (rule-based fallbacks only)
MOCK_LLM=0
SIM_HIGH=0.85
SIM_LOW=0.60
